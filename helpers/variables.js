

let settings = {
    colorMode: ['two-tone', 'white', 'random', 'black-white'],
    backgroundColor: [0, 0, 0],
    debugMode: location.hostname === "",
    rotate: false,
};

let rectangleWave;
let gui;
let rotationSpeed = 1;
let strokeWeigth = 0.1;
let translateX = 0;
let translateY = 0;